// 商品数据（由 parse_posts.py 自动生成，直接嵌入）
const productsData = [
  {"title": "顶级网黄 困困狗 情人节专辑流出 淫乱骚母狗被肌肉猛男无套猛操 第一视角沉浸式性爱日记！【全55.42G】", "subtitle": "颜值:9.5 | 身材:9.7 | 稀缺度:SSR", "size": "55.42G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICNmo41Y-x6USKBW_Sloj7DUpP9Y3xAALOEmsbIGPIVTE_gK2YkwFeAQADAgADeQADPAQ", "cover_local": "", "share_link": "https://share.note.sx/l0reagd7#xtkfug6wcdib/pbWwCRawaNaNAgnwumZobjhxirdfp8", "name": "困困狗", "updated": "2026-05-19T17:48:04+08:00"},
  {"title": "推特符文战士 九条南无 Cos初音未来 粉嫩酮体搭配纹身野辣诱惑 阳具爆插白虎蜜穴美乳颤抖不停【全22.5G】", "subtitle": "颜值:9.6 | 身材:9.8 | 稀缺度:SSR", "size": "22.5G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICP2o41gPl1MszCbAoLzOAF7ekdESAAALYEmsbIGPIVSRHszaEwZ8DAQADAgADdwADPAQ", "cover_local": "QQ20260211-084016.png", "share_link": "https://share.note.sx/2lwbbxjr#LTybsR0tXwfgmzvwPW3OU6LvJaqg1llnQVkXPm+89SA", "name": "九条南无", "updated": "2026-05-19T17:50:54+08:00"},
  {"title": "极品福利姬 Anaimiya奶咪 3月最新福利 骚气热舞扭臀甩胯诱惑无限 丰乳肥臀全景展现风骚无比【全202.88G】", "subtitle": "颜值:9.8 | 身材:9.8 | 稀缺度:SSR", "size": "202.88G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICN2o41fBdJ5wQq1aBIfn6X02PUyi_AALPEmsbIGPIVQsj6FN9K1eHAQADAgADeQADPAQ", "cover_local": "", "share_link": "https://share.note.sx/zfl9ezgi#Q4EPK0vZMstKWZsDUOWSdqssp610IbXCDrcESfyr+Z0", "name": "Anaimiya奶咪", "updated": "2026-05-19T17:48:22+08:00"},
  {"title": "极品长腿黑丝女神 AsiaLynn 户外公厕制服露出诱惑 道具自慰高潮连连！", "subtitle": "颜值:9.8 | 身材:9.8 | 稀缺度:SSR", "size": "273.56G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICOGo41fMFPHp-ZvbiAT_2Jv3jTTG9AALQEmsbIGPIVUd7_4DuoPZxAQADAgADdwADPAQ", "cover_local": "", "share_link": "https://share.note.sx/fy32xwdp#OIB9hNb3hujv4JRbipc9BQ08vjchFpJEvqA4THbQYZU", "name": "Asia-Lynn", "updated": "2026-05-19T17:48:42+08:00"},
  {"title": "台湾知名御姐 辛尤里 自慰私拍大作 摄影师手持道具疯狂挑逗白虎嫩穴 高潮喷水双眼迷离享受酥麻快感【全118.82G】", "subtitle": "颜值:9.8 | 身材:9.8 | 稀缺度:SSR", "size": "118.82G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICOWo41fUwdX9rOO03xkwRGJub4KAyAALSEmsbIGPIVQABi8t9IKs9vQEAAwIAA3cAAzwE", "cover_local": "6ww2p1ni2d9aby5gu5pp1firb0q7zews.jpg", "share_link": "https://share.note.sx/5luzxi9y#jE5Thr6ZEgVTHXCtItxSeXeKGEepk3JUVXU+O9NahDs", "name": "辛尤里", "updated": "2026-03-23T13:56:31+08:00"},
  {"title": "推特网黄 汐梦瑶 大尺度私拍 骚浪小妖精按摩床下偷偷跪舔深喉偷吃肉棒 口爆满嘴浓精一脸满足！【全100.5G】", "subtitle": "颜值:9.8 | 身材:9.8 | 稀缺度:SSR", "size": "100.5G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICOmo41ffn_Dr9GPNQTT2mmV5V8kljAALTEmsbIGPIVbcX1CUKn0XdAQADAgADeQADPAQ", "cover_local": "", "share_link": "https://share.note.sx/9242runb#EaIQ+z3HElARIUC8U0MiqCezkck9Y+djBwJgykcHUJ0", "name": "汐梦瑶", "updated": "2026-05-19T17:49:18+08:00"},
  {"title": "饼干姐姐 淫秽新作 调教绿奴老公 丝袜塞逼封精 口交足交凌辱肉棒 多体位交欢化身精液仓库！【全567.30G】", "subtitle": "颜值:9.8 | 身材:9.8 | 稀缺度:SSR", "size": "567.30G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICO2o41fq5G8Z3QiYkFseMTn1ENUXIAALUEmsbIGPIVU8XYmHDOfPdAQADAgADeQADPAQ", "cover_local": "1u9b92mqt2xs040vfs4xrbefbs46ey39.jpeg", "share_link": "https://share.note.sx/yjgek3fx#MEdi1x1xtS6d7rMdsr5MlKZjXxf4EmvH9OoHVhUzciY", "name": "饼干姐姐", "updated": "2026-05-19T17:49:37+08:00"},
  {"title": "高人气微胖网黄 pupu噗噗 抖音主播被助手拍视频威胁 化身母狗胯下卖力侍奉阳具！【全106G】", "subtitle": "颜值:9.4 | 身材:9.6 | 稀缺度:SSR", "size": "106G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICPGo41fxnNeTuYvlTD_Xw7nKqczBzAALVEmsbIGPIVeL4jJyCT3G4AQADAgADeAADPAQ", "cover_local": "", "share_link": "https://share.note.sx/fr3diy13#CgO1J7EMusQwjKJjD6qLmVeUfuGyEvXn0b6GsUwHnRo", "name": "噗噗", "updated": "2026-05-19T17:49:58+08:00"},
  {"title": "推特网黄 台北娜娜 新作反差婊姐2花絮流出 肉丝腿交口交寸止捆绑狂玩 灵活舌尖狠钻嫩穴 爽到失控！【全784.64G】", "subtitle": "颜值:9.3 | 身材:9.5 | 稀缺度:SSR", "size": "784.64G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICPWo41f6wxAJa2286uHr42ChCGKxwAALWEmsbIGPIVbtRK4LkMj2bAQADAgADeAADPAQ", "cover_local": "", "share_link": "https://share.note.sx/agnw01mb#rLKkfWTm7DrEtaXhE2RDH2e8MF0Y0r0o4jP4wwiC+qI", "name": "台北娜娜", "updated": "2026-05-19T17:50:28+08:00"},
  {"title": "巨乳尤物 御梦子 COS尼尔哈2B性欲挑战 阳具猛插爽出阿黑颜 粉嫩酮体暴力蹂躏 高潮浪叫BUFF叠满！【全37.3G】", "subtitle": "颜值:9.5 | 身材:9.7 | 稀缺度:SSR", "size": "37.3G", "cover_image": "https://tuchuang-aac.pages.dev/api/cfile/AgACAgUAAyEGAATrbgq5AAICPmo41gABTssOk-rLyETqYiT5opd4rwAC1xJrGyBjyFWVvExaAsKRHAEAAwIAA3kAAzwE", "cover_local": "图床/09.御梦子/ia_100000102.jpg", "share_link": "https://share.note.sx/hc2h2848#s9V8SK2UW/rRZ+T1oUvbmg6nTwsibntYofFukbpru2o", "name": "御梦子", "updated": "2026-05-19T17:50:43+08:00"}
];
let posts = JSON.parse(JSON.stringify(productsData));
let slideIndex = 0;
let slideTimer;

// 直接从嵌入的数据加载商品
function loadProducts() {
    renderPosts();
    renderPostsEditList();
}

// ========== 页面加载 ==========
document.addEventListener('DOMContentLoaded', function () {
    document.getElementById('footerYear').textContent = new Date().getFullYear();
    loadProducts();
    loadSavedEdits();
    startSlider();
    bindImageInputs();
});

// ========== 轮播图 ==========
function startSlider() {
    slideTimer = setInterval(() => changeSlide(1), 4000);
}

function changeSlide(dir) {
    const slides = document.querySelectorAll('.slide');
    slides[slideIndex].classList.remove('active');
    slideIndex = (slideIndex + dir + slides.length) % slides.length;
    slides[slideIndex].classList.add('active');
    updateDots();
    resetSliderTimer();
}

function goToSlide(index) {
    const slides = document.querySelectorAll('.slide');
    slides[slideIndex].classList.remove('active');
    slideIndex = index;
    slides[slideIndex].classList.add('active');
    updateDots();
    resetSliderTimer();
}

function updateDots() {
    document.querySelectorAll('.dot').forEach((d, i) => {
        d.classList.toggle('active', i === slideIndex);
    });
}

function resetSliderTimer() {
    clearInterval(slideTimer);
    startSlider();
}

// ========== 移动端菜单 ==========
function toggleMobileMenu() {
    document.getElementById('mobileMenu').classList.toggle('active');
    document.getElementById('mobileMenuOverlay').classList.toggle('active');
}

function toggleSubmenu(e, el) {
    e.preventDefault();
    e.stopPropagation();
    el.parentElement.classList.toggle('open');
}

// ========== 渲染商品列表 ==========
function renderPosts() {
    const grid = document.getElementById('postsGrid');
    if (!posts || posts.length === 0) {
        grid.innerHTML = '<p style="grid-column:1/-1;text-align:center;padding:40px;color:#999;">暂无商品，请确保"商品目录.json"文件存在</p>';
        return;
    }

    grid.innerHTML = posts.map((p, i) => {
        // 解析评分信息
        let looks = '0', figure = '0', rarity = '';
        if (p.subtitle) {
            const parts = p.subtitle.split('|');
            if (parts[0]) looks = parts[0].replace(/颜值[\s:]*/g, '').trim();
            if (parts[1]) figure = parts[1].replace(/身材[\s:]*/g, '').trim();
            if (parts[2]) rarity = parts[2].replace(/稀缺度[\s:]*/g, '').trim();
        }

        return `
        <article class="post-card" data-index="${i}">
            <div class="post-thumb">
                <a href="${escapeHtml(p.share_link || '#')}" target="_blank" class="thumb-link">
                    <img src="${escapeHtml(p.cover_image)}" alt="${escapeHtml(p.name)}" loading="lazy">
                </a>
            </div>
            <div class="post-info">
                <h3><a href="${escapeHtml(p.share_link || '#')}" target="_blank">${escapeHtml(p.name)}</a></h3>
                <div class="post-tags">
                    <span class="tag tag-looks">颜值 ${escapeHtml(looks)}</span>
                    <span class="tag tag-figure">身材 ${escapeHtml(figure)}</span>
                    <span class="tag tag-rarity">${escapeHtml(rarity)}</span>
                </div>
                <div class="post-meta">
                    <span class="post-size"><i class="ri-hard-drive-2-line"></i> ${escapeHtml(p.size || '')}</span>
                    <time>${escapeHtml(p.updated ? new Date(p.updated).toLocaleDateString('zh-CN') : '')}</time>
                </div>
            </div>
        </article>`;
    }).join('');

    // 滚动动画
    if ('IntersectionObserver' in window) {
        const cards = grid.querySelectorAll('.post-card');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.1 });

        cards.forEach((card, i) => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            card.style.transition = `opacity 0.5s ease ${i * 0.05}s, transform 0.5s ease ${i * 0.05}s`;
            observer.observe(card);
        });
    }
}

// ========== 渲染商品编辑列表 ==========
function renderPostsEditList() {
    const container = document.getElementById('postsEditList');
    if (!posts || posts.length === 0) {
        container.innerHTML = '<p style="color:#999;font-size:13px;">暂无商品</p>';
        return;
    }

    container.innerHTML = posts.map((p, i) => `
        <div class="post-edit-item">
            <div class="post-edit-item-header">
                <span>${escapeHtml(p.name || '商品 ' + (i+1))}</span>
                <button onclick="removePost(${i})">删除</button>
            </div>
            <div class="edit-field">
                <label>标题</label>
                <input type="text" value="${escapeHtml(p.title || '')}" onchange="updatePost(${i}, 'title', this.value)">
            </div>
            <div class="edit-field">
                <label>图片URL</label>
                <input type="text" value="${escapeHtml(p.cover_image || '')}" onchange="updatePost(${i}, 'cover_image', this.value)">
            </div>
            <div class="edit-field">
                <label>分享链接</label>
                <input type="text" value="${escapeHtml(p.share_link || '')}" onchange="updatePost(${i}, 'share_link', this.value)">
            </div>
            <div class="edit-field">
                <label>大小</label>
                <input type="text" value="${escapeHtml(p.size || '')}" onchange="updatePost(${i}, 'size', this.value)">
            </div>
        </div>
    `).join('');
}

function escapeHtml(text) {
    if (!text) return '';
    const div = document.createElement('div');
    div.textContent = String(text);
    return div.innerHTML;
}

function updatePost(index, field, value) {
    if (!posts[index]) return;
    posts[index][field] = value;
    renderPosts();
}

function removePost(index) {
    if (confirm('确定删除这个商品吗？')) {
        posts.splice(index, 1);
        renderPosts();
        renderPostsEditList();
    }
}

function addPost() {
    posts.unshift({
        title: "新商品标题",
        cover_image: "https://placehold.co/400x440/CCCCCC/FFFFFF?text=New",
        share_link: "#",
        size: "",
        name: "新商品"
    });
    renderPosts();
    renderPostsEditList();
    setTimeout(() => {
        const list = document.getElementById('postsEditList');
        if (list.firstChild) list.firstChild.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }, 100);
}

// ========== 编辑面板 ==========
function showEditPanel() {
    document.getElementById('editOverlay').classList.add('active');
}

function hideEditPanel() {
    document.getElementById('editOverlay').classList.remove('active');
}


// 绑定图片URL输入
function bindImageInputs() {
    const imgMap = {
        'edit_slide_img_1': 'slide_img_1',
        'edit_slide_img_2': 'slide_img_2',
        'edit_slide_img_3': 'slide_img_3',
        'edit_ad_img': 'ad_img'
    };

    for (const [editId, targetId] of Object.entries(imgMap)) {
        const input = document.getElementById(editId);
        if (input) {
            input.addEventListener('input', function () {
                const img = document.getElementById(targetId);
                if (img && this.value.trim()) {
                    img.src = this.value.trim();
                }
            });
        }
    }
}

// 保存所有编辑
function saveAll() {
    document.querySelectorAll('.edit-group-content input[data-target]').forEach(input => {
        const targetKey = input.dataset.target;
        const value = input.value;
        const editableElements = document.querySelectorAll(`[data-editable="${targetKey}"]`);
        editableElements.forEach(el => { el.textContent = value; });
        localStorage.setItem('edit_' + targetKey, value);
    });

    const slideFields = ['slide_1_title', 'slide_1_author', 'slide_1_date',
                         'slide_2_title', 'slide_2_author', 'slide_2_date',
                         'slide_3_title', 'slide_3_author', 'slide_3_date'];

    slideFields.forEach(field => {
        const input = document.getElementById('edit_' + field);
        if (input && input.value.trim()) {
            localStorage.setItem('edit_' + field, input.value);
        }
    });

    alert('保存成功！刷新页面后内容会保留。');
}

// 加载已保存的编辑
function loadSavedEdits() {
    document.querySelectorAll('[data-editable]').forEach(el => {
        const key = el.dataset.editable;
        const saved = localStorage.getItem('edit_' + key);
        if (saved) el.textContent = saved;
    });

    ['slide_1_title', 'slide_1_author', 'slide_1_date',
     'slide_2_title', 'slide_2_author', 'slide_2_date',
     'slide_3_title', 'slide_3_author', 'slide_3_date'].forEach(field => {
        const saved = localStorage.getItem('edit_' + field);
        if (saved) {
            const input = document.getElementById('edit_' + field);
            if (input) input.value = saved;
        }
    });

    for (let i = 1; i <= 3; i++) {
        const saved = localStorage.getItem('slide_img_' + i);
        if (saved) {
            const img = document.getElementById('slide_img_' + i);
            if (img) img.src = saved;
            const input = document.getElementById('edit_slide_img_' + i);
            if (input) input.value = saved;
        }
    }

    const savedAd = localStorage.getItem('ad_img');
    if (savedAd) {
        const img = document.getElementById('ad_img');
        if (img) img.src = savedAd;
        const input = document.getElementById('edit_ad_img');
        if (input) input.value = savedAd;
    }

    document.querySelectorAll('[data-editable]').forEach(el => {
        const key = el.dataset.editable;
        const input = document.getElementById('edit_' + key);
        if (input) input.value = el.textContent;
    });
}

// 恢复默认
function resetAll() {
    if (!confirm('确定恢复默认内容吗？所有自定义修改将被清除。')) return;
    localStorage.clear();
    location.reload();
}

// 导出HTML
function exportHTML() {
    const clone = document.documentElement.cloneNode(true);
    const editOverlay = clone.querySelector('.edit-overlay');
    if (editOverlay) editOverlay.remove();
    const mobileMenu = clone.querySelector('.mobile-menu');
    if (mobileMenu) mobileMenu.remove();
    const mobileOverlay = clone.querySelector('.mobile-menu-overlay');
    if (mobileOverlay) mobileOverlay.remove();
    const mobileFooter = clone.querySelector('.mobile-footer');
    if (mobileFooter) mobileFooter.remove();

    clone.querySelectorAll('[data-editable]').forEach(el => el.removeAttribute('data-editable'));
    clone.querySelectorAll('.edit-field').forEach(f => f.remove());
    clone.querySelectorAll('.edit-group').forEach(f => f.remove());

    const scriptTags = clone.querySelectorAll('script[src="script.js"]');
    scriptTags.forEach(s => {
        s.remove();
        const newScript = document.createElement('script');
        newScript.textContent = `
            (function(){
                var si=0,slides=document.querySelectorAll('.slide'),dots=document.querySelectorAll('.dot');
                function update(){slides.forEach(function(s,i){s.classList.toggle('active',i===si)});dots.forEach(function(d,i){d.classList.toggle('active',i===si)})}
                function next(){si=(si+1)%slides.length;update();setTimeout(next,4000)}
                if(slides.length>0){update();setTimeout(next,4000)}
            })();
        `;
        s.parentNode.appendChild(newScript);
    });

    const html = '<!DOCTYPE html>\n<html lang="zh-CN">\n' + clone.innerHTML + '\n</html>';
    const blob = new Blob([html], { type: 'text/html;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = '商品目录.html';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
}
